import { Palettes } from "./palettes";
import { Typography } from "./typography";

export const Theme = {
  palettes: {
    ...Palettes,
  },
  typography: {
    ...Typography,
  },
  components: {
    button: {
      
    }
  }
}
